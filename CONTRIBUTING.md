# LanyardStudio 기여 가이드

기여해 주셔서 감사합니다. 작은 문서 수정부터 새 출력 기능까지 모두 환영합니다.

## 시작하기

1. 저장소를 fork하고 작업 브랜치를 만듭니다.
2. Node.js 22 환경에서 `npm ci`를 실행합니다.
3. 변경 범위를 작고 검토 가능하게 유지합니다.
4. 제출 전 `npm run check`를 통과시킵니다.

브랜치 이름은 `feat/`, `fix/`, `docs/`, `refactor/` 같은 짧은 접두사를 권장합니다.

## 설계 원칙

- 파일과 개인정보는 명시적 기능 추가가 없는 한 브라우저 밖으로 보내지 않습니다.
- 화면의 mm 단위와 PDF의 실제 인쇄 크기가 일치해야 합니다.
- 키보드와 포인터 입력을 모두 지원하고, 상태를 색상만으로 전달하지 않습니다.
- 사용자 파일은 신뢰하지 않습니다. 크기, 형식, URL, 수치 범위를 검증합니다.
- 새 의존성은 번들 크기, 라이선스, 유지보수 상태를 설명할 수 있을 때만 추가합니다.

## Pull request

PR 본문에 다음 내용을 포함해 주세요.

- 해결하려는 문제
- 구현 방식과 주요 판단
- 검증한 명령어
- UI 변경 전후 이미지(해당되는 경우)
- 개인정보, 파일 import, PDF 출력에 미치는 영향

보안 취약점은 공개 이슈로 등록하지 말고 [SECURITY.md](SECURITY.md)를 따라 주세요.
